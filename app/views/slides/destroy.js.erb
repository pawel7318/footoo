$('<%= @destroyed_slides.map { |slide| "#slide_" + slide.id.to_s }.join(', ') %>').remove();
$('<%= @destroyed_slides.map { |slide| "#slide_move_" + slide.id.to_s }.join(', ') %>').remove();
$('<%= @destroyed_slides.map { |slide| "#slide_delete_" + slide.id.to_s }.join(', ') %>').remove();
$('#flash_messages').append('<%= j render('layouts/flash') %>')
<% flash.discard %>
